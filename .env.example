# URL du webhook n8n qui reçoit les messages du chat (agent commercial NeoTravel).
# Sans cette variable, /api/chat répond en mode dégradé (503) et invite à utiliser le simulateur.
N8N_WEBHOOK_URL=

# URL publique du site (ex: https://neotravel.vercel.app)
NEXT_PUBLIC_APP_URL=http://localhost:3000
